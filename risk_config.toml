[weights]
# Risk scoring weights (higher means more risk added to the total score)
cve_critical_weight = 40
cve_high_weight = 25
cve_medium_weight = 10
cve_low_weight = 5

# Maintenance health
abandoned_sdk_weight = 20
stale_sdk_weight = 10

# Permission scope-creep
permission_creep_weight = 15

# Native binary flags
suspicious_binary_import_weight = 10
packed_binary_weight = 30

# Network endpoints
malicious_network_endpoint_weight = 50
